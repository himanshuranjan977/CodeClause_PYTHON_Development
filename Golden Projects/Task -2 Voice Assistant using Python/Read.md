These are the basic key to install to make a voice assiatant

# pip install wikipedia

# pip install pyttsx3

# pip installspeech_recognition as sr

# pip install datetime

# pip install wikipedia

# pip install pywhatkit

# pip install os

# pip install smtplib

# pip install pyaudio
